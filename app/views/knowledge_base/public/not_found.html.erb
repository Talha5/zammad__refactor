<main class="main main--error">
  <div class="container">
    <%= icon('mood-sad') %>
    <h1><%= tts_t('Page not found') %></h1>
    <p>
      <%= tts_t('The page you were looking for does not exist.') %>
      <%= link_to tts_t('Click here'), custom_path_if_needed(help_root_path(locale: fallback_locale.locale), @knowledge_base) %>
      <%= tts_t('to go to the homepage.') %>
    </p>
  </div>
</main>
